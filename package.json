{
  "name": "endpoint-directory-challange",
  "version": "1.0.0",
  "description": "CLI tool to simulate a file system directory tree",
  "main": "index.js",
  "scripts": {
    "start": "npm run compile && cd build node index.js",
    "build": "tsc --outDir out --sourcemap index.ts",
    "lint": "npx eslint .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rob LaCagnina",
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^9.10.0",
    "@types/eslint__js": "^8.42.3",
    "eslint": "^9.10.0",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.6.0"
  }
}
